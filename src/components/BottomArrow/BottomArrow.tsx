import classes from './BottomArrow.module.scss';
import React, { FC } from 'react';

const BottomArrow: FC = () => {
  return <div className={classes.bottomArrow}>arrow</div>;
};

export default BottomArrow;
